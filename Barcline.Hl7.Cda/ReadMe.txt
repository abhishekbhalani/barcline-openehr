README

The project contains slightly improved classes for XML/JSON serialization/deserialization (all arrays are substituted 
with typed collections whenever possible, except for IVL_* classes with xs:choice-s)
